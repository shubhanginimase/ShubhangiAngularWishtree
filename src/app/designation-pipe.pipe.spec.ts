import { DesignationPipePipe } from './designation-pipe.pipe';

describe('DesignationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DesignationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
