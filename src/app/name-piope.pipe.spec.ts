import { NamePiopePipe } from './name-piope.pipe';

describe('NamePiopePipe', () => {
  it('create an instance', () => {
    const pipe = new NamePiopePipe();
    expect(pipe).toBeTruthy();
  });
});
